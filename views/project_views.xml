<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Case 1 : Item 3a
         Create new list view for Sites -->
    <record id="project_project_view_tree_site" model="ir.ui.view">
        <field name="name">project.project.tree.site</field>
        <field name="model">project.project</field>
        <field name="arch" type="xml">
            <tree string="Sites">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="user_id"/>
                <field name="deadline_date"/>
                <field name="budget"/>
                <field name="project_size"/>
                <field name="stage_id"/>
                <field name="task_count"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </tree>
        </field>
    </record>

    
    <!-- Case 1 : Item 3b, 3c
     Override the main action to use list view as default -->
    <record id="project.open_view_project_all" model="ir.actions.act_window">
        <field name="name">Sites</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">tree,kanban,form,calendar,activity</field>
        <field name="view_id" ref="project_project_view_tree_site"/>
        <field name="search_view_id" ref="project.view_project_project_filter"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No sites found. Let's create one!
            </p><p>
                Create sites to organize your milestones.
            </p>
        </field>
    </record>

    <record id="project.open_view_project_all_group_stage" model="ir.actions.act_window">
        <field name="name">Sites</field>
        <field name="res_model">project.project</field>
        <field name="context">{'search_default_groupby_stage': 1}</field>
        <field name="domain">[]</field>
        <field name="view_mode">tree,kanban,form,calendar,activity</field>
        <field name="view_id" ref="project_project_view_tree_site"/>
        <field name="search_view_id" ref="project.view_project_project_filter"/>
        <field name="target">main</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No sites found. Let's create one!
            </p>
            <p>
                Sites contain tasks on the same topic, and each has its own dashboard.
            </p>
        </field>
    </record>

    <!-- Case 1 : Item 4
      Inherit project form view to add new fields -->
    <record id="project_project_view_form_inherit" model="ir.ui.view">
        <field name="name">project.project.form.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="deadline_date"/>
                <field name="budget"/>
                <field name="project_size"/>
            </xpath>
        </field>
    </record>
</odoo>